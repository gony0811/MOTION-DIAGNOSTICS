<UserControl x:Class="MDS.UI.UserControls.TableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*" />
            <ColumnDefinition Width="0.7*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBox Text="{Binding DeviceConfig.DeviceName}" Grid.ColumnSpan="2" FontSize="20" FontWeight="Bold" BorderThickness="0" Background="Transparent" Margin="8" />

        <TextBox Text="DeviceType" Grid.Column="0" Grid.Row="1" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.DeviceType}" Grid.Column="1" Grid.Row="1" />
        <Border Grid.ColumnSpan="2" Grid.Row="1" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <TextBox Text="InstanceName" Grid.Column="0" Grid.Row="2" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.InstanceName}" Grid.Column="1" Grid.Row="2" />
        <Border Grid.ColumnSpan="2" Grid.Row="2" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <TextBox Text="FileName" Grid.Column="0" Grid.Row="3" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.FileName}" Grid.Column="1" Grid.Row="3" />
        <Border Grid.ColumnSpan="2" Grid.Row="3" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <TextBox Text="Use" Grid.Column="0" Grid.Row="4" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.IsUse}" Grid.Column="1" Grid.Row="4" />
        <Border Grid.ColumnSpan="2" Grid.Row="4" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <TextBox Text="Args" Grid.Column="0" Grid.Row="5" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.Args}" Grid.Column="1" Grid.Row="5" />
        <Border Grid.ColumnSpan="2" Grid.Row="5" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <TextBox Text="Description" Grid.Column="0" Grid.Row="6" IsReadOnly="True" />
        <TextBox Text="{Binding DeviceConfig.Description}" Grid.Column="1" Grid.Row="6" />
        <Border Grid.ColumnSpan="2" Grid.Row="6" Height="1" Background="Gray" VerticalAlignment="Bottom" />

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" Grid.Column="1" Grid.Row="7" HorizontalAlignment="Right">
            <Button Content="저장" Command="{Binding SaveCommand}" Margin="0,0,10,0" />
            <Button Content="삭제" Command="{Binding DeleteCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
