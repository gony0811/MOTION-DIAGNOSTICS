<UserControl x:Class="MotionDiagnostics.UserControls.RelativeMoveControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="200">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"></Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10">
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="27*"/>
                <RowDefinition Height="40*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.6*"></ColumnDefinition>
                <ColumnDefinition Width="0.4*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Grid.RowSpan="2" >
                <Label Content="Picth"></Label>
                <ComboBox>
                    <ComboBoxItem>
                        <TextBlock Text="mm"/>
                    </ComboBoxItem>
                </ComboBox>
                <TextBox MinWidth="150"></TextBox>
                <Button HorizontalAlignment="Right" Content="Move" Margin="10"></Button>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
