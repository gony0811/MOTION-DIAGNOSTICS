<UserControl x:Class="MotionDiagnostics.UserControls.AbsoluteMoveControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Height="200">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}"></Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition></ColumnDefinition>
                <ColumnDefinition></ColumnDefinition>
            </Grid.ColumnDefinitions>


            <Label Grid.Column="0" Grid.Row="0" Content="Position #1"></Label>
            <TextBox Grid.Column="0" Grid.Row="1" MinWidth="100" Margin="10"></TextBox>
            <Button Grid.Column="0" Grid.Row="2" HorizontalAlignment="Right" Content="Move" Margin="10"></Button>

            <Label Grid.Column="1" Grid.Row="0" Content="Position #2"></Label>
            <TextBox Grid.Column="1" Grid.Row="1" MinWidth="100" Margin="10"></TextBox>
            <Button Grid.Column="1" Grid.Row="2" HorizontalAlignment="Right" Content="Move" Margin="10"></Button>

        </Grid>

    </Border>
</UserControl>
